import * as React from 'react';

import { HeaderTitle, HeaderStyled } from './styles';

const Header = () => {
  return <HeaderStyled>
      <HeaderTitle>
        <span>Hello</span>
      </HeaderTitle>
  </HeaderStyled>;
};

export default Header;
