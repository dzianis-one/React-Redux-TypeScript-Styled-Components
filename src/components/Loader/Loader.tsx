import * as React from 'react';

import { Ilustration, LoaderWrapper } from './styles';

const Loader = () => {
  return <LoaderWrapper>
    <Ilustration />
  </LoaderWrapper>;
};

export default Loader;
